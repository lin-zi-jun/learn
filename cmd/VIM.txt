:set  nu     //显示行号
:set nonu    //取消显示行号
u   撤销上一步的操作
Ctrl + r: do again

ctags -R 
set tags+=/home/helloworld/tags

//cursor movement
k            上移                k
h            左移            h        l
l            右移                j
j            下移

gg，跳到文本的第一行
shift+g，跳到文本的最后一行
行首:shift + 6  
行尾:shifr + 4

:sp file2 //水平分割打开第二个文件，sp=split
:vsp file2 //垂直分割打开第二个文件，vsp=vsplit

:files  #列出当前打开的所有文件

多窗口编辑的窗口光标切换：ctrl + w + 上下的方向键

Ctrl + a：将光标移到所在行首(head)
Ctrl + e：将光标移到所在行尾（end）

delete:x
rm \n : J
